name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pipeline:
    runs-on: ubuntu-latest

    services:
      db:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: lacda
          POSTGRES_PASSWORD: lacda_pass
          POSTGRES_DB: loans
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U lacda -d loans"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DB_USER: lacda
      DB_PASSWORD: lacda_pass
      DB_HOST: localhost
      DB_PORT: 5432
      DB_NAME: loans

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Initialise schema
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
          PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f db/init.sql

      - name: Ingesta
        run: python scripts/ingesta.py

      - name: Quality check (KPIs sobre dato crudo)
        run: python scripts/qualitycheck.py

      - name: Limpieza
        run: python scripts/limpieza.py

      - name: Transformacion
        run: python scripts/transformacion.py

      - name: Validacion y carga (gate de calidad)
        run: python scripts/validacion.py

      - name: Install ML dependencies
        run: pip install scikit-learn matplotlib seaborn

      - name: Entrenar modelo (lee desde loan_data en Postgres)
        run: python scripts/train_model.py

      - name: Evaluar modelo (gate de calidad ML, AUC >= 0.93)
        run: python scripts/test_model.py

      - name: Upload CSVs procesados
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: csvs-procesados
          path: |
            data/loan_data_clean.csv
            data/loan_data_transformed.csv
          if-no-files-found: warn

      - name: Upload resultados del modelo
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: resultados-modelo
          path: results/
          if-no-files-found: warn
